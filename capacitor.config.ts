import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.armaklan.companion',
  appName: 'warcrow-companion',
  webDir: 'dist/warcrow-companion/browser'
};

export default config;
